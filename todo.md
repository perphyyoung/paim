# todo

本文件是临时性文件，其它文件不得引用它。现状与规划见 [缓存及加载优化设计.md](缓存及加载优化设计.md)。

## 缓存与加载优化

设计目标：**图像/提示词支撑万级数据（1 万条常驻流畅、10 万条可用）**，分页为默认路径；**标签按小规模设计（每域 500 上限）**，不做万级处理。

全部完成。已完成清单（详见设计文档 §2.1 / §2.4 / §3 / §9）：后端 `list_*_page` / `list_*_ids` / `*_special_counts` 查询下推 + 部分索引、前端 `usePagedBlocks`（块 200 / 上限 25 块 / 可见块 ±1 预取 / `seq` 防串台）、两主页分块列表 + 300ms 关键词防抖 + 后端特殊标签计数、两主页缩略图映射按块并与块淘汰同步、`reload()` 保留占位高度以免丢滚动位置、提示词背景自愈改为按提示词校验（`ensure_prompt_thumbnails`，修掉「传提示词 id 给图像 id 校验」的错配；缩略图路径统一为相对值、前端拼数据目录）、标签改为每域 500 上限（达上限拒绝新增、渲染回归全量）、Rust 单测覆盖分页/筛选/特殊标签/缩略图/标签上限、前端单测覆盖块窗口与竞态（`pnpm test:ui`，`usePagedBlocks.test.ts`：`seq` 防串台 / 失败自动重试 / ±1 预取 / LRU 淘汰 / 占位长度）、e2e 分页专项（`07-paged-blocks`：块大小压到 2 造多块场景，覆盖跨块滚动时远端块按需补齐）、万级压测通过（脚本见 `scripts/readme.md`；压测中修复特殊标签计数/筛选 SQL 退化 151s→90ms，查询命令迁移 `spawn_blocking` 后台执行）、P3 字段瘦身（`ImageCard` / `PromptCard` 列表投影，`stored_name` / `relative_path` / `md5` / `gen_params` / `is_deleted` 不出库，`stored_name` 仅后台拼路径与排序键，前端显示统一 `file_name`）。
