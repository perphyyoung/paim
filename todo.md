# todo

本文件仅供临时性的进度追踪，其它文件不得引用。

## 嵌套详情的「槽位」模型（已实施，后续项保留）

**要修的问题**：图像详情 → 相似结果页 → 点提示词结果 → 打开的提示词详情是**嵌套态**（父级传了 `is-nested`），
其内容右键的菜单项被 `v-if="similarityEnabled && !isNested"` 挡掉 → 右键弹出的是**空盒子**（看起来没反应）。
同时嵌套层**缺 `@open-*` 接线**，所以直接放开菜单会变成「点了没反应」（同类结果）或「再叠一层」（跨类结果）。

**目标模型**（最底层的原始详情永不被替换）：

```
第 0 层  原始详情（页面级）              ← 从主页打开的那个详情，全程不动
第 1 层  第 0 层的「跨类」嵌套槽（各一个）  ← 图像详情里嵌一个提示词详情 / 反之
第 2 层  第 1 层的「跨类」嵌套槽（各一个）  ← 最多再一层
```

规则（对任何一层都成立）：

- **同类结果** → 换掉**那一类已有的那个槽**（顶层详情由页面换，保持既有语义）；
- **跨类结果** → 用**本层自己的跨类槽**（已有内容则替换，不新开层）。

⇒ 层数恒 ≤ 3、第 0 层永不被替换、图像/提示词嵌套各最多一个。

**接线清单**（已全部落地）

- [x] `PromptDetailModal.vue` 内容右键菜单项：`v-if` 去掉 `!isNested`（保留 `similarityEnabled`）
- [x] `PromptDetailModal.vue` 里嵌套的 `ImageDetailModal`：补 `@open-image="onOpenSimilarImage"`（同类 → 替换本层嵌套图像）
      与 `@open-prompt="onNestedOpenPrompt"`（跨类 → 关掉嵌套图像并上抛，由宿主替换它的嵌套提示词槽）
- [x] 新增 `PromptDetailModal.onNestedOpenPrompt(id)`（关 `imgDetailOpen` + `emit("open-prompt", id)`，2 行）
- [x] `ImageDetailModal.vue` 里嵌套的 `PromptDetailModal`：补 `@open-prompt="onOpenSimilarPrompt"`（同类 → 替换本层嵌套提示词，复用现成函数）

**已知语义**：替换外层槽会连带关掉它里面那层 —— 层级立刻收敛，不会「越点越多」。

**后续（本轮不做）**：**放开二级跳转** —— 嵌套态下「查看图像详情」/「编辑提示词」按钮目前禁用（title 写着「禁止二级跳转」）。
按上面的槽位模型，它们也能安全放开（都落在「各自一个槽」里）；届时去掉 `isNested` 对这两个入口的禁用即可，
`isNested` 只保留「不再叠加同类详情」的语义（或整体退场）。

**验证**（已完成）：`e2e/11` 新增用例 —— 嵌套层内容右键可见 → 跨类结果落在本层槽位（底部详情不动、上一层保留）
→ 同类结果只替换不叠加 → 逐层关闭后底部详情原样；修复前该用例必红。
可复用的长期说明写在 `docs/lessons.md` 第 24 节（本文件不参与引用）。
