# todo

本文件是临时性文件，其它文件不得引用它。现状与规划见 [缓存及加载优化设计.md](缓存及加载优化设计.md)。

## 缓存与加载优化

设计目标：**图像/提示词支撑万级数据（1 万条常驻流畅、10 万条可用）**，分页为默认路径；**标签按小规模设计（每域 500 上限）**，不做万级处理。

已完成（详见设计文档 §2.1 / §2.4 / §3 / §9）：后端 `list_*_page` / `list_*_ids` / `*_special_counts` 查询下推 + 部分索引、前端 `usePagedBlocks`（块 200 / 上限 25 块 / 可见块 ±1 预取 / `seq` 防串台）、两主页分块列表 + 300ms 关键词防抖 + 后端特殊标签计数、两主页缩略图映射按块并与块淘汰同步、`reload()` 保留占位高度以免丢滚动位置、提示词背景自愈改为按提示词校验（`ensure_prompt_thumbnails`，修掉「传提示词 id 给图像 id 校验」的错配；缩略图路径统一为相对值、前端拼数据目录）、标签改为每域 500 上限（达上限拒绝新增、渲染回归全量）、Rust 单测覆盖分页/筛选/特殊标签/缩略图/标签上限。

### 剩余事项

1. **e2e 分页专项**（`pnpm e2e`）：现有用例数据量小（落在单块内）路径等价且未用到关键词搜索，无需改造；新增——用 `localStorage.paim.blockSize` 设为 2~5 造多块场景，覆盖跨块滚动、块失败自动重试、条件切换 `seqId` 防串台。
2. **跑一次万级压测并记录数据**（脚本已就位）：`node scripts/bench-data.mjs seed --dir paim-data.压测 --images 10000 --prompts 10000 --tags 500`，量四项——首屏可见、连续滚动是否空洞、排序切换、搜索响应；DevTools heap 常驻应贴近「已加载块数 × 块体积」而非全量。跑完 `clean` 清理。

### P3 字段瘦身（备选，仅在分页后内存仍不满足时做）

卡片只保留轻字段投影，`gen_params` / `stored_name` 等详情才用的字段按需取。`note` 的搜索已下推到 SQL（`filter_sql`），投影里可以去掉。预计可再降 30%~50% 常驻内存，UX 无影响。

