# 虚拟滚动可选优化

> 当前状态:图像/提示词两页已接入 VirtualGrid(虚拟滚动)+ CustomScrollBar(自定义滚动条);
> 页面加载已做:缩略图 URL 直接由行内 thumbnail_path 构建(消除 N+1 IPC)、
> 大集合 shallowRef(浅响应)、加载并行化、路由级 KeepAlive(切页保留数据与滚动位置,对齐 pm 行为)。
> 本文记录**暂缓**的后续优化,供数据量上涨时参考。

## 数据分页(暂不做)

现状:`list_images` / `list_prompts` 一次全量加载。虚拟滚动已把渲染钉死在 O(可视区),
剩余成本是全量数据传输(万级 ≈ 几 MB JSON,百毫秒级)与内存排序,一次性发生、可接受。

参考 lap 的分页体系(为 10 万级文件库设计,复杂度高):

1. 首查只取 total,把列表造成等长占位数组(如 `{ isPlaceholder: true }`);
2. VirtualGrid 在窗口变化时 emit 可视区间(start/end);
3. 视口驱动的分块拉取,三阶段优先级:视口 → 视口下方 → 视口上方,seqId 防竞态、分块去重;
4. 占位回填时保留已有字段,避免闪烁。

**触发条件**:数据量到数万条,或实测全量加载超过 ~300ms 再做。

**演进路径**(平滑,不破坏现有结构):

- Rust 侧加 `count_images/count_prompts` + `list_*_paged(offset, limit, sort)` 命令;
- 前端把 VirtualGrid 的 `items` 换成占位数组,监听窗口区间拉取回填;
- VirtualGrid 本身无需改动(它只消费数组)。

## 其他小项

- **缩略图缓存穿透参数(?v=)**:仅当缩略图会被原地重生成时需要;当前重建缩略图
  会重写 thumbnail_path 并整表刷新映射,天然失效,不需要。
- **Pinia 集中状态**:路由缓存当前用 KeepAlive + 页内状态已够;当跨页共享的派生状态
  变多(全局选中集、标签筛选跨页联动等)再引入。
