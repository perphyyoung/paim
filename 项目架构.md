# 项目架构

paim（文生图提示词及图像管理）的**结构唯一事实源**：目录实况、分层边界、关键链路走向、质量门。分层规则的定义文件是 [.sentrux/rules.toml](.sentrux/rules.toml)，由 `sentrux check .` 强制执行；本文与之保持一致，并记录结构检查的当前状态。

本文不管「怎么装怎么跑」（见 [README.md](README.md)）、不管命令链路与踩坑 SOP（见 [AGENTS.md](AGENTS.md) 与 `docs/`）、也不记录改动历史（见 [CHANGE.md](CHANGE.md)）。

## 总体原则

- **业务逻辑全部在 Rust 后端**，Vue 只做展示与参数传递，经 Tauri commands 调用。
- **特征切片**：按业务纵向切分（prompt / image / tag / backup），同一业务在两端对齐，可单独增删。前端是 `src/features/<业务>/` 目录；后端是 `commands/` 与 `domain/` 下的同名文件对（`prompt.rs` + `prompt_service.rs`），层由目录决定，业务由文件前缀决定。
- **后端目录即层名**：`commands/`（命令层）→ `domain/`（领域层）→ `infra/`（基础设施），新文件放错目录会被 `sentrux check .` 拦下。
- **类型安全边界**：命令与事件经 tauri-specta 生成 `src/bindings.ts`，前端不手写 invoke。
- **分层单向依赖**：低层不引用高层（见下文分层表），由 `sentrux check` 强制。

## 目录结构

``` dir
src/                            # Vue 前端
├── main.ts / App.vue / router/ # 应用根：入口、根组件、路由
├── bindings.ts                 # tauri-specta 生成，禁止手改
├── views/                      # 页面：PromptPage / ImagePage / SettingsView
├── features/                   # 特征切片（前端）
│   ├── prompt/                 #   api/prompt.ts + components(4 个弹窗)
│   ├── image/                  #   api/thumbnails.ts + useThumbnailSelfHeal + components(5 个弹窗/查看器)
│   ├── tag/                    #   标签组件与 composable（图像/提示词共用）
│   └── backup/                 #   pm 备份导入（api + 弹窗）
├── components/                 # 共享组件与组合式函数（MediaCard、VirtualGrid…）
├── composables/                # 共享 composable（批量选择、快捷键…）
└── utils/                      # 纯工具（日期、字体、日志…）

src-tauri/src/                  # Rust 后端（目录即层名：commands → domain → infra）
├── main.rs                     # 入口
├── lib.rs                      # 组合根：模块声明 + 命令/事件注册表（specta_builder）
├── commands.rs                 # 命令层模块声明
├── commands/                   # 命令层（order 2）：薄壳，只做参数校验 + 调 domain
│   ├── prompt.rs(+test)        #   提示词命令
│   ├── image.rs                #   图像命令
│   ├── prompt_tag.rs           #   提示词标签命令（与 image_tag 对称）
│   ├── image_tag.rs            #   图像标签命令
│   └── pm_backup.rs            #   pm 备份导入命令
├── domain.rs                   # 领域层模块声明
├── domain/                     # 领域层（order 1）：不感知 Tauri，可单测
│   ├── prompt_service.rs(+test)        #   提示词领域逻辑
│   ├── image_service.rs(+test)         #   图像领域逻辑
│   ├── image_ops.rs                    #   纯图像处理：image:: 调用唯一入口
│   ├── thumbnail_service.rs(+test)     #   缩略图重建/补缺
│   ├── tag_manager.rs                  #   TagDomain 泛化 CRUD（两套标签共用）
│   └── pm_backup_service.rs(+test)     #   pm 备份导入实现
├── infra.rs                    # 基础设施模块声明
└── infra/                      # 基础设施（order 0）：不引用任何上层
    ├── db.rs(+test)            #   连接管理、schema、数据/临时目录、防呆
    ├── error.rs                #   AppError（序列化为可读字符串）
    ├── logging.rs              #   paim.log 写入 + log_msg 前端上报
    └── text_utils.rs(+test)    #   LIKE 转义等纯函数

e2e/                            # Playwright e2e（CDP 连真实应用）
scripts/gen-bindings.mjs        # pnpm check 复写 bindings.ts

<项目根>/                        # 运行期产物（非源码）
├── paim-data/                  # 数据目录：paim.db + 图像 + 缩略图
├── temp/                       # 临时目录
└── paim.log                    # 三类日志统一落盘
```

## 分层与依赖方向

`order` 越小越基础，依赖只能从大 order 流向小 order（低层禁止引用高层）：

| order | 层            | 路径                                                            | 说明                            |
| ----- | ------------- | --------------------------------------------------------------- | ------------------------------- |
| 0     | rust-infra    | `src-tauri/src/infra/**`                                        | 连接/schema、错误、纯函数、日志 |
| 1     | rust-domain   | `src-tauri/src/domain/**`                                       | 领域逻辑，不感知命令层          |
| 2     | rust-commands | `src-tauri/src/commands/**`、`lib.rs`、`main.rs`                | 薄命令 + 组合根                 |
| 3     | web-bindings  | `src/bindings.ts`                                               | 生成文件，零内部依赖            |
| 4     | web-shared    | `src/components/**`、`src/composables/**`、`src/utils/**`       | 共享件，不依赖业务切片          |
| 5     | web-features  | `src/features/**`                                               | 业务切片                        |
| 6     | web-views     | `src/views/**`                                                  | 页面                            |
| 7     | web-app       | `src/main.ts`、`src/App.vue`、`src/router/**`、`src/version.ts` | 应用根                          |
| 9     | e2e           | `e2e/**`、`scripts/**`                                          | 测试与脚本，可引用任意层        |

点名禁令（boundaries，与 order 无关）：`src/components/** ✗ src/features/**`、`src/bindings.ts ✗ src/**`、`src/** ✗ e2e/**`。

### 命名约定

外部 crate `image` 与本地模块 `commands::image` 同名，图像处理调用一律经 `domain/image_ops.rs`，其它模块不直接写 `image::`（避免同名歧义，见 [CHANGE.md](CHANGE.md) 的 v0.2.14）。

### 结构检查（sentrux check）

规则表达的是**目标架构**，现有违规是待修事项，不通过放宽规则消除。

## 关键链路

### 命令与类型绑定（tauri-specta）

命令枚举与 TS 类型的单一事实源是 `lib.rs` 的 `specta_builder()`，前端经生成的 `src/bindings.ts` 调用，不手写 invoke。新增/修改命令的流程与排查见 [docs/新增命令说明(tauri-specta 版).md](docs/新增命令说明(tauri-specta 版).md)。

### 数据目录与数据集切换

- `infra/db.rs::base_data_dir`：`PAIM_DATA_DIR`（e2e 隔离）优先 → debug 用项目根 `paim-data` → release 用系统应用数据目录（`com.paim.perphyyoung`）。
- 临时目录与数据目录**分离**（`<项目根>/temp`、release 在应用缓存目录）：运行中临时文件下次启动清空；`preview-<tag>` 为实例预览目录。
- 数据集切换靠目录改名（`paim-data.名字` 为备用集），用户侧操作步骤、启动防呆与 pm 备份的 `paim-data_{时间戳}` 命名规则见 [README.md](README.md)。

### pm 备份导入

`domain/pm_backup_service.rs`：zip → **系统临时目录**解压 → 切内存连接（释放 paim.db 锁）→ 数据目录整体改名让位（`paim-data_<时间戳>`）→ ATTACH 备份库单事务灌入 → 流式恢复图像 → 缩略图全量重建 → 失败自动回滚（删半成品、备份归位、重开原库）。

其中**缩略图重建的性能现状与备选方案**见 [docs/导入优化.md](docs/导入优化.md)；数据目录改名规则见 [README.md](README.md)。

### 缩略图

导入时内联生成 + `thumbnail_service` 重建/补缺，两者共用 `image_ops::make_center_thumb`；前端 `features/image/useThumbnailSelfHeal` 在卡片加载失败时触发自愈。

### e2e 测试

CDP 连真实应用（`--remote-debugging-port`），每 worker 独立实例 + 独立数据目录（`PAIM_DATA_DIR=temp/e2e-w<n>`）；文件选择对话框用 debug 构建的测试缝 `select_images`（`PAIM_E2E_MOCK_IMAGE_PATHS`）。运行与约定见 [docs/e2e测试.md](docs/e2e测试.md)。

### 日志

三类日志（Rust log 宏、前端 log_msg 上报、e2e logger）统一写根目录 `paim.log`；e2e 的使用见 [docs/日志使用说明.md](docs/日志使用说明.md)。

## 质量门

命令的环境前提与踩坑（target 目录、vite 监听白名单、e2e workers 调优等）见 [AGENTS.md](AGENTS.md)。

| 命令              | 覆盖范围                                                               |
| ----------------- | ---------------------------------------------------------------------- |
| `pnpm check`      | 前端 + Rust 全链路校验（含自动复写 bindings.ts），链路细节见 AGENTS.md |
| `pnpm test`       | Rust 单元测试（`*.test.rs` 与源文件平铺）                              |
| `pnpm e2e`        | Playwright（CDP 连真实应用，每 worker 一个实例）                       |
| `sentrux check .` | 分层方向、点名禁令、环、圈复杂度、上帝文件                             |

## 相关文档

- [README.md](README.md)：技术栈、支持的图像格式、快速开始、数据集切换操作、数据库 Schema。
- [AGENTS.md](AGENTS.md)：编码约定、`pnpm check` 等命令链路细节、环境踩坑、`docs/` 索引。
- [CHANGE.md](CHANGE.md)：逐版本改动记录（含本轮分层归位与除环）。
- [缓存及加载优化设计.md](缓存及加载优化设计.md)：全盘缓存与加载优化盘点（按页面/场景组织），姊妹文档见 docs/虚拟滚动可选优化.md、docs/导入优化.md。
- [docs/readme.md](docs/readme.md)：`docs/` 目录索引，按主题查细则。
