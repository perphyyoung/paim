# 项目架构

paim（文生图提示词及图像管理）的架构说明与分层约定。分层规则的定义文件是
[.sentrux/rules.toml](.sentrux/rules.toml)，由 `sentrux check .` 强制执行；
本文与之保持一致，并记录结构检查的当前状态。

## 总体原则

- **业务逻辑全部在 Rust 后端**，Vue 只做展示与参数传递，经 Tauri commands 调用。
- **特征切片**：按业务纵向切分（prompt / image / tag / backup），前端与后端各有一个
  `features/`，同一业务在两端对齐，可单独增删。
- **类型安全边界**：命令与事件经 tauri-specta 生成 `src/bindings.ts`，前端不手写 invoke。
- **分层单向依赖**：低层不引用高层（见下文分层表），由 `sentrux check` 强制。

## 目录结构（实际）

```
src/                            # Vue 前端
├── main.ts / App.vue / router/ # 应用根：入口、根组件、路由
├── bindings.ts                 # tauri-specta 生成，禁止手改
├── views/                      # 页面：PromptPage / ImagePage / SettingsView
├── features/                   # 特征切片（前端）
│   ├── prompt/                 #   api/prompt.ts + components(4 个弹窗)
│   ├── image/                  #   api/thumbnails.ts + components(5 个弹窗/查看器)
│   ├── tag/                    #   标签组件与 composable（图像/提示词共用）
│   └── backup/                 #   pm 备份导入（api + 弹窗）
├── components/                 # 共享组件与组合式函数（MediaCard、VirtualGrid…）
├── composables/                # 共享 composable（批量选择、快捷键…）
└── utils/                      # 纯工具（日期、字体、日志…）

src-tauri/src/                  # Rust 后端
├── main.rs / lib.rs            # 入口；命令注册表 + 组合根（DI、事件挂载）
├── features.rs                 # features 模块声明
├── features/                   # 特征切片（后端）
│   ├── prompt.rs / prompt_service.rs(+test)      # 提示词：命令 / 领域逻辑
│   ├── image.rs / image_service.rs(+test)        # 图像：命令 / 领域逻辑
│   ├── prompt_tag.rs / image_tag.rs              # 两套标签管理命令
│   ├── tag_manager.rs                            # TagDomain 泛化 CRUD（两套标签共用）
│   ├── thumbnail_service.rs(+test)               # 缩略图重建/补缺
│   └── pm_backup.rs / pm_backup_service.rs(+test)# pm 备份：命令 / 导入实现
├── db.rs(+test)                # 连接管理、schema、数据/临时目录、防呆
├── error.rs                    # AppError（序列化为可读字符串）
├── logging.rs                  # paim.log 写入 + log_msg 前端上报
└── text_utils.rs(+test)        # LIKE 转义等纯函数

e2e/                            # Playwright e2e（CDP 连真实应用）
scripts/gen-bindings.mjs        # pnpm check 复写 bindings.ts
```

## 分层与依赖方向

`order` 越小越基础，依赖只能从大 order 流向小 order（低层禁止引用高层）：

| order | 层 | 路径 | 说明 |
| --- | --- | --- | --- |
| 0 | rust-infra | `src-tauri/src/db*`、`error.rs`、`text_utils*`、`logging.rs` | 连接/schema、错误、纯函数、日志 |
| 1 | rust-domain | `features/*_service*`、`tag_manager*` | 领域逻辑，不感知命令层 |
| 2 | rust-commands | `features/{image,prompt,prompt_tag,image_tag,pm_backup}.rs`、`features.rs`、`lib.rs`、`main.rs` | 薄命令 + 组合根 |
| 3 | web-bindings | `src/bindings.ts` | 生成文件，零内部依赖 |
| 4 | web-shared | `src/components/**`、`src/composables/**`、`src/utils/**` | 共享件，不依赖业务切片 |
| 5 | web-features | `src/features/**` | 业务切片 |
| 6 | web-views | `src/views/**` | 页面 |
| 7 | web-app | `src/main.ts`、`src/App.vue`、`src/router/**`、`src/version.ts` | 应用根 |
| 9 | e2e | `e2e/**`、`scripts/**` | 测试与脚本，可引用任意层 |

点名禁令（boundaries，与 order 无关）：`src/components/** ✗ src/features/**`、
`src/bindings.ts ✗ src/**`、`src/** ✗ e2e/**`。

## 结构检查现状（sentrux check，2026-09-06）

规则表达的是**目标架构**，现有违规是待修事项，不通过放宽规则消除：

### ① 前端共享层依赖业务切片（已修复，2026-09-06）

原违例：`components/useThumbnailSelfHeal.ts` → `features/image/api`（业务行为放错共享层），
以及 `MediaCard.vue` → `features/image/components/CardTagRow.vue`（共享组件引用业务组件）。
修复：`useThumbnailSelfHeal` 移入 `features/image/`；`TagChip`（注释即「通用标签胶囊」，
纯 props + 字符串插槽）与 `CardTagRow`（string 标签截断行）上移到 `src/components/`，
共享层不再依赖任何业务切片。

### ② 环 `image.rs ↔ image_service.rs ↔ thumbnail_service.rs`（max_cycles）

经核实，**源码层面不存在回边**：`image_service` / `thumbnail_service` 均未引用 `image.rs`；
真实边为 `image.rs → image_service`、`image.rs → thumbnail_service`、
`thumbnail_service → image_service::make_center_thumb`（`:80`），是有向无环的。
最可疑的假边：`thumbnail_service.rs:79` 的 `image::open` 是**外部图像处理 crate `image`**，
与本地命令模块 `features::image` 同名，疑被按名解析归并成本地依赖。需按「临时构造违规、
确认能否命中」的方法验证；若坐实误报，通过重命名本地模块或上报工具解决。

不过这里确实存在一个真实的耦合点，建议随重构一并处理：**缩略图职责分散两处**——
`image_service` 在导入时内联生成缩略图，`thumbnail_service` 负责重建/补缺并反向调用
`image_service::make_center_thumb`；此外 `pm_backup_service → thumbnail_service::rebuild_all`
也是领域内横向依赖。单向依赖不构成环、当前合法，但把纯图像处理助手（解码/裁剪/编码）
抽到独立模块（如 `features/image_ops.rs`）可让缩略图职责单源、消除横向网。

## 关键链路

### 命令与类型绑定（tauri-specta）

- 单一事实源：`lib.rs` 的 `specta_builder()`，同时供 `invoke_handler` 与 TS 绑定导出。
- 两条生成路径：① `pnpm dev` debug 启动自动导出 `src/bindings.ts`；② `pnpm check` 经
  `scripts/gen-bindings.mjs` 以「导出即退」模式（环境变量 `PAIM_EXPORT_BINDINGS`）复写。
  详细流程见 [docs/新增命令说明(tauri-specta 版).md](docs/新增命令说明(tauri-specta 版).md)。

### 数据目录与数据集切换

- `db.rs::base_data_dir`：`PAIM_DATA_DIR`（e2e 隔离）优先 → debug 用项目根 `paim-data` →
  release 用系统应用数据目录（`com.paim.perphyyoung`）。
- 临时目录与数据目录**分离**（`<项目根>/temp`、release 在应用缓存目录）：运行中临时文件
  下次启动清空；`preview-<tag>` 为实例预览目录。
- 数据集切换：目录改名（`paim-data.名字` 为备用集），启动防呆见 README。

### pm 备份导入

`pm_backup_service.rs`：zip → **系统临时目录**解压 → 切内存连接（释放 paim.db 锁）→
数据目录整体改名让位（`paim-data_<时间戳>`）→ ATTACH 备份库单事务灌入 → 流式恢复图像 →
缩略图全量重建 → 失败自动回滚（删半成品、备份归位、重开原库）。

### 缩略图

导入时内联生成 + `thumbnail_service` 重建/补缺；前端 `useThumbnailSelfHeal` 在卡片
加载失败时触发自愈（待迁移，见上①）。

### e2e 测试

CDP 连真实应用（`--remote-debugging-port`），每 worker 独立实例 + 独立数据目录
（`PAIM_DATA_DIR=temp/e2e-w<n>`）；文件选择对话框用 debug 构建的测试缝
`select_images`（`PAIM_E2E_MOCK_IMAGE_PATHS`）。运行与约定见
[docs/e2e测试.md](docs/e2e测试.md)。

### 日志

三类日志（Rust log 宏、前端 log_msg 上报、e2e logger）统一写根目录 `paim.log`；
e2e 级别经 `.env.e2e` 配置。见 [docs/日志使用说明.md](docs/日志使用说明.md)。

## 质量门

- `pnpm check`：format → build:rs → gen:bindings → typecheck → build（自动复写 bindings）。
- `pnpm test`：Rust 单元测试（`*.test.rs` 与源文件平铺）。
- `pnpm e2e`：Playwright（CDP，多 worker 并行）。
- `sentrux check .`：分层方向、点名禁令、环、圈复杂度、上帝文件。
