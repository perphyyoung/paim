# 缓存及加载优化设计

> 本文档盘点项目**全部**缓存与加载优化机制，按「**哪个页面/场景用到**」组织；跨页面的基础设施单列在顶部。
> 姊妹文档：`docs/虚拟滚动可选优化.md`（暂缓项）、`docs/导入优化.md`（导入期缩略图生成）。

## 0. 总览

| 优化项                | 所属页面/场景     | 类型           | 关键文件或机制                                                |
| --------------------- | ----------------- | -------------- | ------------------------------------------------------------- |
| 路由 KeepAlive        | 全局（两主页）    | 页面实例缓存   | `App.vue`                                                     |
| 跨页脏标记            | 全局（跨页失效）  | 失效策略       | `utils/crossPageCache.ts`                                     |
| localStorage 持久化   | 全局（UI 偏好）   | 状态免重算     | `gridColumns.ts` / `font.ts` / `TagFilterPanel.vue`           |
| 拖拽单例回调          | 全局（标签→卡片） | 复用 / 免重载  | `useTagDragToCard.ts`                                         |
| 标签候选仓库          | 全局（标签输入）  | 模块级单例缓存 | `useTagCandidates.ts`                                         |
| 虚拟滚动              | 图像/提示词主页   | 渲染窗口化     | `VirtualGrid.vue` / `useGridScrollSync.ts`                    |
| 窗口化分页 + 查询下推 | 图像/提示词主页   | 内存/加载上限  | `usePagedBlocks.ts` / 后端 `list_query.rs`                    |
| 批量 Map 命令         | 图像/提示词主页   | IPC 削减       | `commands/tag.rs` / `commands/{image,prompt}.rs`              |
| 缩略图行内构建        | 图像/提示词主页   | IPC 削减       | 图像列表行内 `thumbnail_path`；提示词按块 `getPromptThumbs`    |
| 并行加载 + shallowRef | 图像/提示词主页   | 加载提速       | 各主页 `Promise.all` / `shallowRef`                           |
| 缩略图懒自愈          | 图像/提示词主页   | 按需生成       | `useThumbnailSelfHeal.ts`                                     |
| 实体级缓存 + 预取     | 详情弹窗          | 缓存命中       | `entityCache.ts` / `relatedImagesCache.ts` / `detailCache.ts` |
| 关联标签批量查询      | 详情弹窗（后端）  | SQL N+1→2      | `tag_manager.rs` / `prompt_service.rs` / `image_service.rs`   |
| 全屏惰性解析          | 全屏查看器        | 惰性加载       | `ImageFullscreenViewer.vue`                                   |
| 行内缩略图 + 防抖     | 选择器弹窗        | 加载提速       | `ImagePickerModal.vue`                                        |

## 1. 全局基础设施（跨页面）

这些机制不绑定单一页面，是其它优化得以成立的前提。

### 1.1 路由 KeepAlive（页面实例缓存）

`src/App.vue` 用 `<KeepAlive>` 包裹 `<RouterView>`：两主页实例常驻内存，切页时**保留数据与滚动位置**，数据仅首次进入时加载。配套「刷新缓存」按钮（`reloadAll()`）走整页 `location.reload()`，作为所有缓存状态的终极重置。

### 1.2 跨页脏标记（失效策略）

`src/utils/crossPageCache.ts`：模块级 `Set<PageKey>`（`"images"` / `"prompts"`）。

- `markPageStale(page)`：某页操作改变了另一页展示的数据时置脏（约 30+ 处调用，如 `PromptPage` 写操作 → `markPageStale("images")`）。
- `consumePageStale(page)`：被 KeepAlive 缓存的页面在 `onActivated` 中消费，命中才 `loadImages/loadPrompts`。
- 设计意图：缓存实例后不自动重拉，靠脏标记在**正确的时机**触发一次重载，避免无谓全量刷新，也避免跨页数据不一致。

### 1.3 localStorage 持久化（UI 状态免重算）

把影响体验的偏好持久化，刷新/重进页不重算、不闪默认值：

| 状态                  | Key                                             | 位置                               |
| --------------------- | ----------------------------------------------- | ---------------------------------- |
| 显示列数（钳制 2–12） | `image.columns` / `prompt.columns`              | `utils/gridColumns.ts`             |
| 排序字段 / 方向       | `SORT_KEY` / `SORT_DESC_KEY`                    | `ImagePage.vue` / `PromptPage.vue` |
| 全局 / 详情字号       | `font.ts` 内键                                  | `utils/font.ts`                    |
| 标签筛选区折叠 / 排序 | `${domain}.tagFilterCollapsed` / `TAG_SORT_KEY` | `TagFilterPanel.vue`               |
| 卡片信息条显隐        | `cardInfoVisible`                                 | `utils/cardInfo.ts`                |

> 容量：约 15 个 key（含两域各 4 个），每 key 存布尔/数字/短字符串，总计 < 200 B；硬上限是 WebView2 的 localStorage 配额（Chromium 口径 ~5 MB，UTF-16 计约 10 MB），且写入无 try-catch，超限会抛 `QuotaExceededError`。当前用量距上限 4 个数量级，无需处理。

### 1.4 拖拽单例回调（复用 / 免重载）

`src/features/tag/useTagDragToCard.ts`：模块级单例 `onDrop` 回调（`registerCardTagDrop`），两主页被 KeepAlive 缓存共用。页面在 `onActivated/onDeactivated` 里 `activate/deactivate` 切换，**不重复注册全局 pointer 监听**；拖拽添加成功后**本地合并 `tagNames`**（`useTagDragToCard.ts`），即时反映到卡片，不触发整页重载。

### 1.5 标签候选仓库（自动完成数据源）

`src/features/tag/useTagCandidates.ts`：模块级单例 `ref<string[]>`，供三处标签输入（图像详情 / 提示词详情 / 批量添加弹窗）共用。候选**不区分域**——图像域 + 提示词域标签合并去重，`localeCompare("zh")` 升序。

- **复用已有加载**：主页 `loadTagFilter()` 拉到本域标签后调 `mergeTagNames(domain, names)` 并入候选（零额外请求），`mergedDomains: Set` 记录已合并的域；`ensureTagCandidates()` 只对缺失的域惰性发 `getTagData(domain)`（Promise 去重防并发），两域齐备后不再发请求。输入框聚焦即触发 ensure，首次使用时通常已被主页预热。
- **本地补名免重拉**：添加标签成功后 `addTagName(name)`（`useTagAdd` / `useBatchTagAdd` / `useTagDragToCard` 三处调用），新标签即时进入候选，不重新拉取。
- **失效时机**（白名单）：标签管理保存（`onTagManagerSaved` → `invalidateTagCandidates()`，随 `loadTagFilter` 重建）、数据导入恢复。详情页删除标签不失效（标签本体未变）。
- 消费方 `TagAutocompleteInput.vue` 做前缀过滤（内存内，无命令）；输入值以组件内部镜像 ref 驱动过滤与提交——直接依赖 props 会有父组件重渲染延迟（首字符丢候选）。
- **容量无上限**：条数 = 两域标签名去重后的全集（实测 174 条关系 → 候选 ≤174，约 5 KB；两域各 500 上限时约 30 KB，量级恒小）。每次 `merge` 是「重建 Set + 全量 `localeCompare` 排序」的 O(n log n) 操作，这个规模无需增量合并；回收只有 `invalidateTagCandidates()` 整体清空。
- **无需为规模降级**：候选数组本身很小；渲染侧消费方 `TagAutocompleteInput` 恒定只渲染前 20 条命中项（`max`），且标签本体按「每域 500 上限」设计（见 §9），候选集天然有界。

## 2. 图像主页 ImagePage

进入页面后，「渲染 → 数据拉取 → 缩略图」三层各自优化。

### 2.1 虚拟滚动 + 滚动位置恢复

`src/components/VirtualGrid.vue`（参考 lap VirtualScroll / pm VirtualScroller）：phantom wrapper 撑高 + 可见项 absolute 定位，只渲染可视窗口 + 上下缓冲行（`buffer=2`）；滚动用 `requestAnimationFrame` 节流，`ResizeObserver` 重测尺寸。配套的 `useGridScrollSync.ts` 在 KeepAlive 激活时 `restoreSaved()` 恢复 `savedTop`，筛选/排序后 `backToTop()`。

> 容量（唯一与数据量无关的硬上限）：DOM 节点数 = `columns × (visibleRows + 2×buffer)`，`buffer=2`；列数钳制 2–12、单卡最小 80 px。例：8 列 × 视口 4 行 → 常驻 64 张卡片，1 万条数据也不变。phantom 高度只占一个 div，不随条数增节点。

### 2.2 批量 Map 命令（一次 IPC 返回全量映射）

后端按 id 聚合的命令，前端拿 `Record<id, ...>` 后本地查表，消除逐条目请求。标签类命令已按域合一（`commands/tag.rs`，首参 `domain`）：

| 命令（调用）           | 返回                                                                                                                               |
| ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| `getTagsMap("image")`  | `HashMap<String, Vec<String>>` 图像卡片标签                                                                                        |
| `getTagData("image")`  | `{groups, tags[count]}` 筛选区一次取全（组 + 带未删除计数的标签），替代原 `list_all_image_tags` + `list_image_tag_groups` 两次请求 |
| `getImagePromptsMap()` | `HashMap<String, Vec<String>>` 图像卡片关联提示词文案                                                                              |
| `imageSpecialCounts()` | `HashMap<String, i64>` 特殊标签命中数（收藏/未引/多引/无标/安全/敏感），6 个 COUNT 一次回                                                 |

调用点：`ImagePage.vue.loadTagFilter`（2 命令，原 3 个）、`ImagePage.vue.loadImages` 与 `loadSpecialCounts`。角标计数直接取后端 `count`（只统计未删除图像），不再前端自算；特殊标签计数改由 `imageSpecialCounts` 提供，替代原先对全量数组 `filter` 的自算。

> 容量：`getTagsMap` / `getImagePromptsMap` 返回的是全量映射，前端按 id 全量驻留，条数 = 数据集规模（上限与回收方式见 §7）；`images` 列表本身已改为按块持有、不再全量驻留（见 §2.4）。

### 2.3 缩略图行内构建（消除 N+1 IPC）

`list_images_page`（主页分页列表）与 `list_images`（选择器）行内返回 `thumbnail_path`，前端直接 `convertFileSrc(\`${dir}/${thumbnail_path}\`)` 拼出 URL，**不再逐图发命令取路径**。提示词卡片背景取「关联首图」，无法随列表行内返回，改由 `getPromptThumbs(ids)` 按块取（`PromptPage` 的 `pageItems` watch 补齐已加载块未取过的 id），映射容量与块缓存同量级。

### 2.4 窗口化分页（按块持有）+ 并行加载 + shallowRef

列表不再全量驻留：`usePagedBlocks` 只保留「已加载块」并做 LRU 淘汰，其余位置用占位对象填充，定长数组长度 = 后端 `total`，因此 VirtualGrid 的窗口计算与定位无需改动。

- 单块 **200 条**，常驻上限 **25 块（5000 条）**，可见块 ±1 预取，淘汰时保护可见块 ±2（详见 §7）。块大小可用 `localStorage.paim.blockSize` 覆盖（e2e 测试缝）。
- 块级行为的测试分层：跨块补齐（远端块按需渲染）由 `e2e/07` 覆盖（压块到 2 造多块；序号见 [docs/e2e测试.md](docs/e2e测试.md) 的文件索引）；`seq` 防串台、失败自动重试、±1 预取、LRU 淘汰由前端单测 `src/composables/usePagedBlocks.test.ts`（`pnpm test:ui`）覆盖——真实 Tauri 的 IPC 层不可打补丁做故障注入，理由见 [docs/e2e测试.md](docs/e2e测试.md)。
- 排序 / 搜索 / 标签筛选 / 特殊标签计数**全部下推到 SQL**（`domain/list_query.rs` + `image_service::list_page`/`list_ids`/`special_counts`）：分页后前端拿不到全量，无法再在内存里排序与过滤。
- 条件管道：关键词 300 ms 防抖（`debouncedKeyword`），排序 / 标签 / 反选立即触发；任一条件变化 → `reload()`（自增 `seq` 丢弃过期响应）→ 拉首屏块 → `backToTop()`。
- 全选 / 反选 / 批量改走 `listImageIds`（同条件只回 id，单次上限 2 万），不再依赖前端全量集合。
- `reload()` 不清零 `total`、不截断数组，只把已知槽位重置为占位：保留旧高度，KeepAlive 恢复滚动位置时才有高度可落（否则条件刷新会把位置丢回顶部）；新 `total` 到达后由块响应校正。
- 缩略图映射与块同生共死：`pageItems` watch 只为本块条目拼 URL，并丢掉离开已加载块的键，避免映射随滚动过的条目无限增长。

`loadImages` 仍用 `Promise.all([getImagePromptsMap, getDataDir])` 并行拉取卡片附加数据，随后 `reloadBlocks()` + `loadSpecialCounts()`。`pageItems` / `thumbs` / `tagNames` / `imagePrompts` 均为 `shallowRef`，大数组只整体替换触发更新，避免深层响应式代理开销。

> 容量：列表条数 = **已加载块数 × 200 ≤ 5000**（与数据集规模脱钩）；`getTagsMap` / `getImagePromptsMap` 仍是全量映射，随数据集线性增长（万级下为待优化项，见 §8）。**回收站不是第二份全量**：`trashImages`/`trashThumbs` 走 `list_trashed_images`（后端 `WHERE is_deleted = 1`），只是软删除条目的独立小集合（通常 ≤10 条），关闭遮罩即清空。

### 2.5 缩略图懒自愈（按需生成）

`src/features/image/useThumbnailSelfHeal.ts`（参考 pm `ensureImageThumbnails`）：可见窗口稳定后（防抖 500ms），把**当前可见项 id** 批量发后端校验；缺图且原图存在才按需生成并回写。每个 id 只校验一次（`checked: Set`），`inflight` 守卫避免重入，`onDeactivated` 清定时器；`onFixed` 仅重建对应缩略图 URL 映射，不动整表。

## 3. 提示词主页 PromptPage

与图像主页对称（同为「窗口化分页 + 查询下推 + 虚拟滚动」，见 §2.4），但映射命令集不同。

### 3.1 虚拟滚动 + 滚动恢复

同 §2.1（`VirtualGrid` + `useGridScrollSync`）。

### 3.2 批量 Map 命令

| 命令（调用）             | 返回                                                                             |
| ------------------------ | -------------------------------------------------------------------------------- |
| `getTagsMap("prompt")`   | `HashMap<String, Vec<String>>` 卡片标签                                          |
| `getTagData("prompt")`   | `{groups, tags[count]}` 筛选区一次取全                                           |
| `getPromptThumbs(ids)`   | `HashMap<String, String>` 卡片背景缩略图，只回传入的提示词（按块取）              |
| `promptSpecialCounts()`  | `HashMap<String, i64>` 特殊标签命中数（收藏/多图/无图/无标/单语/安全/敏感），一次回 |

列表数据由 `listPromptsPage` / `listPromptIds` 按块与按条件提供（`prompt_service::{list_page,list_ids,special_counts}`）；原 `getPromptImagesCountMap` 已随特殊标签计数下推而删除。

### 3.3 缩略图按块取 + 懒自愈

提示词卡片背景取「关联首图」，无法随列表行内返回（缩略图属于关联图像而非提示词行），因此由 `getPromptThumbs(ids)` 按块取：`pageItems` watch 收集已加载块内未请求过的 id 批量取一次，`thumbTried` 记住已请求过的（含取不到背景的）避免反复请求；块淘汰或数据重载时一并清掉 URL 与请求记忆，映射规模随块缓存有界。可见窗口稳定后的懒自愈同 §2.5（修复后按提示词强制重取）。

> 已知遗留：提示词页的 `useThumbnailSelfHeal` 传的是**提示词 id**，而后端 `ensure_image_thumbnails` 收的是**图像 id**，该校验目前不会命中任何图像、修不出缩略图；修法见 `todo.md`。

## 4. 详情弹窗（提示词 / 图像）

### 4.1 实体级缓存（模块级 + in-flight 去重）

痛点：`PromptPage.vue` 用 `v-if="detailOpen"` 强制卸载弹窗 → 组件内 ref 活不过一次开关，任何组件内缓存都失效。因此缓存必须挂在**模块作用域**。

`src/utils/entityCache.ts` 抽出 `createEntityCache<T>(load, maxEntries)` 工厂，提供 `get` / `fetch` / `invalidate`：`fetch` 命中 `inflight` Map 则复用同一 Promise，同 id 并发只发一次命令；`maxEntries` 为 LRU 条数上限（命中重新入队尾，超限淘汰最久未用），各实例上限见下表。基于它实例化：

| 缓存                             | 类型              | 命令                       |
| -------------------------------- | ----------------- | -------------------------- |
| `relatedImagesCache`（提示词侧） | `RelatedImage[]`  | `getPromptRelatedImages`   |
| `relatedPromptsCache`（图像侧）  | `LinkedPrompt[]`  | `getImageRelatedPrompts`   |
| `imageSrcCache`（图像侧）        | `string` 原图路径 | `getImageSrc`              |
| `imageTagsCache`（图像侧）       | `TagLite[]`       | `getItemTags("image", id)` |

使用约定：弹窗 `loadX()` 先 `get(id)` 命中则直接渲染（不进 loading、无闪烁），未命中才 `fetch(id)` 回填；数据变化走 `invalidate(id)` 后重 `fetch`。失效时机（白名单，别漏）：提示词侧移除关联 / 设为首图 / 导入 / 嵌套换图；图像侧解除关联 / 新建提示词 / 嵌套提示词 updated / 标签增删 / 安全评级联动。**保存提示词字段不失效**（不影响关联关系）。

> 容量按实例 LRU 封顶（`createEntityCache(load, maxEntries)`）：命中即移到队尾，超限淘汰最久未用（Map 保持插入序）。上限为 `relatedImagesCache` / `relatedPromptsCache` **100** 条（单条 1–10 KB → 约 1 MB 封顶）、`imageSrcCache` / `imageTagsCache` **300** 条（单条 < 1 KB）。弹窗关闭与切页**不释放**（模块级活到应用结束），数据新鲜度仍靠 `invalidate(id)` 白名单保证；LRU 淘汰只降低命中率，不影响正确性——未命中即重新读库。§4.2 的邻项预取在满容量时会挤掉最久未用项，翻回去多一次本地 IPC（< 10 ms）。

### 4.2 切换时预取邻项

`PromptDetailModal` 切换条目后，对 `order[currentIndex ± 1]` 里未缓存的 id 后台补 `fetch`，连续翻页几乎全命中。

### 4.3 后端关联标签批量查询（N+1 → 2 条 SQL）

原 `prompt_service::list_related_images`（`src-tauri/src/domain/prompt_service.rs`）在循环内 prepare 标签查询，N 张图 = N+1 次 SQL；图像侧 `get_image_related_prompts`（原在 `commands/image.rs`）同构。

重构后：标签查询统一为 `domain/tag_manager.rs::tags_by_owner(conn, TagDomain, ids)`——一次 `IN (...)` 批量查后按 id 分组回填。两侧列表函数只剩「取 id → `tags_by_owner` → 回填」三行；`get_image_related_prompts` 查询逻辑下沉到 `image_service::list_related_prompts`，命令层留薄壳。N+1 条 SQL 降为 2 条（`list` + 批量 `tags_by_owner`）。

### 4.4 〔相关〕保存按需字段（写入路径优化）

`PromptDetailModal` / `ImageDetailModal` 的 `saveFields()` 在前端逐字段判定，**只传变化的字段**（未变传 `null`，全未变早退不发命令），后端按 `Some` 字段直写。属于「提交负载」优化，与上面的「读取缓存」互补；标注在此避免遗漏。注意判定必须用 `=== null`，清空备注/翻译传的是 `""`，真值判断会误判漏存。

## 5. 全屏查看器 ImageFullscreenViewer

`src/features/image/components/ImageFullscreenViewer.vue`：大图 `src` 与元信息（name/tags）通过 `resolveSrc` / `resolveMeta` **函数注入**，按 id 惰性解析；`srcs` / `metas` 用 `Record` 缓存已解析结果，翻看过的项不重复请求。

> 容量：单次打开内条数 = 翻看过的图数（约 100 B/条），无逐条淘汰；但组件由 `v-if="fullscreenOpen"` 挂载（`ImageDetailModal.vue:1049` / `PromptDetailModal.vue:973`），**关闭全屏即整体释放**，不会跨会话累积——这是它与 §4.1 `entityCache` 的关键差别。真正的内存大头是 `<img>` 解码位图，由 WebView 自行回收，不在本缓存口径内。

## 6. 选择器弹窗 ImagePickerModal

`src/features/prompt/components/ImagePickerModal.vue`：缩略图 URL 由列表行内 `thumbnail_path` 本地 `convertFileSrc` 拼出（`loadImages` 内 `Promise.all([listImages, getDataDir])` 并行取），不再逐图发 `getImageThumbnail`——N+1 IPC 归零，loading 在列表返回即结束。搜索/标签变化仍用 300ms 防抖（`ImagePickerModal.vue`）。

## 7. 各缓存容量与上限速查

硬上限有三类：localStorage 配额（~5 MB）、VirtualGrid 的 DOM 窗口、以及两个 LRU 封顶的条目缓存（`entityCache` §4.1、主页块缓存 §2.4）；其余缓存**无条数上限、无 TTL**，靠显式失效或组件/页面销毁回收。下表实测基准为 2026-09-10 的 `paim-data`：700 图像 / 525 提示词 / 174 条标签关系 / 缩略图 700 张 6.3 MB / `paim.db` 1.4 MB。

| 缓存（对应节）                       | 载体                          | 条数上限                                                     | 实测占用（700 图 / 525 提示词）                                | 回收方式                                              | 增长风险                    |
| ------------------------------------ | ----------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------ | ----------------------------------------------------- | --------------------------- |
| KeepAlive 页实例 §1.1                | 组件实例 + 页内全量数据      | 固定 2（两主页，路由表封顶）                                 | 单页约 1–3 MB（含下表全部映射）                              | 仅 `location.reload()`                                | 低（数量硬编码，不随数据） |
| 跨页脏标记 §1.2                      | `Set<PageKey>`                | 2（枚举封顶）                                                | < 100 B                                                      | `consumePageStale` 消费即删                           | 无                          |
| localStorage 偏好 §1.3               | WebView2 localStorage         | **配额 ~5 MB（硬上限，UTF-16 计约 10 MB）**                  | 约 15 个 key，合计 < 200 B                                   | 手动清理；无自动过期                                  | 低（写入无 try-catch）      |
| 标签候选 §1.5                        | `string[]`（模块级单例）      | = 两域标签名去重全集（每域 ≤500，见 §9）                     | ≤174 条，约 5 KB（上限规模约 30 KB）                         | `invalidateTagCandidates()` 整体清空                  | 低（规模有界）             |
| 虚拟滚动窗口 §2.1                    | DOM 节点                      | **`columns × (visibleRows + 2×buffer)`，`buffer=2`**         | 8 列 × 视口 4 行 = 64 节点；1 万条数据不变                   | 滚动即复用                                            | 无（与数据量无关）         |
| 主页列表块缓存 §2.4 / §3.1           | `Map<块号, 条目[]>`（模块级 LRU） | **25 块 × 200 条 = 5000 条**，可见块 ±2 不淘汰               | 单条含对象头约 860 B（图像）/ 1.2 KB（提示词）→ 约 4.3 / 6 MB | 超限淘汰最久未进入可见区的块；条件变化整体清空        | 低（已封顶）               |
| 主页附加映射 §2.2 / §3.2             | `shallowRef` Record           | `thumbs` **随块缓存有界**（两主页均与块淘汰同步）；`tagNames`/`imagePrompts` 无上限 = 数据集规模 | `thumbs` ≤5000 条约 0.6 MB；`tagNames`/`imagePrompts` 仅覆盖有关系的条目 | 随块淘汰 / 页重载清空；实例随 KeepAlive 常驻          | 低 / 中（`tagNames`/`imagePrompts` 仍随数据集线性增长） |
| 主页定长列表数组 §2.4                | `shallowRef` 定长数组（含占位） | = 后端 `total`（占位对象每条约 20 B）                        | 1 万条占位约 0.2 MB；已加载位置指向块内条目（不复制）         | 条件变化整体替换                                      | 低（占位极轻）             |
| 回收站集合（两主页 `loadTrash`）     | `shallowRef`（`is_deleted=1`） | = 软删除条目数（通常 ≤10，非全量副本）                       | < 5 KB                                                       | 关闭遮罩即清空；下次打开重新拉取                      | 无                          |
| 实体级缓存 §4.1（4 个实例）          | `Map`（模块级，LRU 封顶）   | 每实例固定上限：`relatedImages`/`relatedPrompts` 100 条、`imageSrc`/`imageTags` 300 条 | 翻遍 700 条不再无限增长：两类分别封顶约 1 MB / 0.3 MB         | `invalidate(id)` 显式删 + 超限时淘汰最久未用          | 低（已封顶）               |
| 全屏 `srcs`/`metas` §5               | `Record`（组件内）            | 单次打开内翻看过的图数                                       | 约 100 B/条                                                  | `v-if` 关闭全屏随组件销毁                             | 无                          |
| 自愈 `checked` §2.5                  | `Set<string>`                 | ≤ 本页图像总数                                               | 700 × 36 B ≈ 25 KB                                           | `resetChecked()`（数据重载）/ 页实例销毁              | 无                          |
| 磁盘缩略图 §2.3                      | `thumbnails/{YYYYMM}/*.jpg`   | 无上限 = 图像记录数                                          | 700 张 6.3 MB（9.5 KB/张，200×200 JPEG q80）→ 1 万图约 95 MB | 硬删除记录时删文件（`image_service.rs:443`）；软删除保留 | 中（无容量/时间淘汰）      |
| SQLite 隐式                          | WAL + 单连接 page cache       | WAL 自动 checkpoint 1000 页 ≈ 4 MB；page cache 未显式设置（SQLite 默认 ~2 MB） | WAL 实测 1 MB，`paim.db` 1.4 MB                              | checkpoint / 连接关闭（单连接 + Mutex，`infra/db.rs`） | 低                          |
| 重建/导入并发峰值（后端，非缓存）    | 解码线程持有的位图            | `workers = min(总数, available_parallelism)`（本机 16）      | 峰值 ≈ 线程数 × 单张解码位图（4000×3000 RGB ≈ 36 MB → 理论 ~570 MB） | 重建结束即释放                                        | 中（详见 `docs/导入优化.md`） |

结论（现状）：

- 两处按 LRU 封顶的条目缓存在命中时都会「重新入队尾 / 触碰」，超限只淘汰最久未用者；淘汰只降低命中率、不影响正确性（未命中重新读库，失效仍走各自白名单）。`entityCache` 靠 `invalidate(id)`，主页块缓存靠条件变化整体清空。
- 主页**列表**已封顶（≤5000 条，与数据集脱钩）；两主页的缩略图映射也与块淘汰同步（≤5000 条）。`getTagsMap` / `getImagePromptsMap` 仍是全量 Record（随数据集线性增长），但万级下仍在可接受量级，**不做分页下推**（理由见 §9）。
- 主页定长数组长度为后端 `total`，未加载位置是极轻的占位对象（约 20 B），随数据量线性增长但可忽略。
- 回收站是软删除条目的临时集合（通常 ≤10 条），关闭遮罩即释放，不存在「两份全量」问题。
- 磁盘侧（缩略图）不设淘汰：单张约 10 KB，且随记录硬删除一并清理。

## 8. 规划中未实施

均为「数据量上涨或硬性性能要求时」再启动（部分来自 `docs/虚拟滚动可选优化.md` 与 `docs/导入优化.md`）：

- **主页字段瘦身**（备选）：卡片只保留轻字段投影，`gen_params` / `stored_name` 等详情才用的字段按需取。`note` 的搜索已下推到 SQL（`filter_sql`），投影里可以去掉。
- **Pinia 集中状态**：当前 KeepAlive + 页内状态已够；跨页派生状态（全局选中集、标签筛选跨页联动）变多时再引入。
- **缩略图缓存穿透 `?v=`**：当前重建缩略图会重写 `thumbnail_path` 并整表刷新映射，天然失效，不需要。
- **导入期缩略图**：`jpeg-encoder`（SIMD）已替换默认编码器落地；`turbojpeg` / `libvips` 备选因 C 依赖与发行复杂度暂缓；满核并发（`available_parallelism`）已启用。

## 9. 数据规模支持

目标：**图像 / 提示词按 1 万条常驻流畅、10 万条可用**设计，不做「低于阈值走全量」的双模式。
**标签不在此列**：标签数按小规模（每域 500 上限）设计，全量渲染即可，见下表与取舍说明。

| 数据 | 瓶颈在哪 | 做法 | 关键文件 |
| --- | --- | --- | --- |
| 提示词、图像 | 全量驻留内存 + 前端排序/过滤要拿全量 | 窗口化分页（块 200 条 / 上限 25 块 / 可见块 ±1 预取 / LRU 保护 ±2），排序 / 搜索 / 标签筛选 / 特殊标签计数全部下推 SQL，配 `WHERE is_deleted = 0` 的部分索引 | `usePagedBlocks.ts`、`domain/list_query.rs`、`{image,prompt}_service::list_page` |
| 标签 | 数据量小（远小于条目集合），瓶颈只在 DOM | **不做万级处理**：全量返回、全量渲染，不做分批渲染；**每域 500 上限**（达上限拒绝新增，自动创建路径一并拦截）+ **首位组 100 上限**（防止卡片区被挤出视口），约束细节见根目录 readme.md | `TagFilterPanel.vue`、`TagManagerModal.vue`、`domain/tag_manager.rs`（`MAX_TAGS_PER_DOMAIN` / `MAX_TAGS_IN_TOP_GROUP`）、`domain/tag_service.rs` |
| 缩略图 | 提示词背景不在列表行内（属于关联图像） | 按**已加载块**批量取（`getPromptThumbs(ids)`，后端回相对路径、前端拼数据目录），映射随块淘汰同步收缩；懒自愈按提示词校验（`ensurePromptThumbnails`），只回传路径变化的提示词 | `PromptPage.vue`、`prompt_service::{thumbs_for, ensure_thumbnails}` |

取舍说明：

- **标签为什么不做万级、也不分页**：标签数据量比「带标签的条目集合」小一到两个数量级，全量返回的 IPC 与内存都不构成瓶颈；分页会引入分块拉取、批量添加预检退化（`tagNames[id]` 可能不在已加载块内）、标签改名后的大范围失效等复杂度，收益不成比例。曾按「全量返回 + 搜索 + 渲染上限（筛选区 300 chip / 弹窗每批 300）」设计，后确认**标签不需要万级**：改为每域 **500** 上限，超出即在新建入口直接提示，渲染侧回归全量（首位组 100 上限除外，约束统一见根目录 readme.md；筛选区无搜索框，管理弹窗保留）。上限只拦新增命令，导入 / 恢复不受限。
- **固定预算**：块缓存 ≤5000 条（约 4.3 MB 图像 / 6 MB 提示词）、缩略图映射 ≤5000 条（约 0.6 MB）、`entityCache` 4 实例各自 LRU 封顶、标签每域 ≤500（候选集与筛选区 chip 因此恒小）。全部与数据集规模脱钩，只有 `getTagsMap` / `getImagePromptsMap` 仍随数据量线性增长（万级下几百 KB 量级）。
- **提示词背景自愈**：可见窗口稳定后按提示词 id 交给后端（`ensure_prompt_thumbnails`），后端解析到关联图像再补缩略图，返回背景路径变化的提示词集合；早期版本误把提示词 id 传给按图像 id 校验的命令，等于没做自愈，已修正。

压测：`node scripts/bench-data.mjs seed --dir <数据目录> [--images N] [--prompts N] [--tags N]` 灌入 id 带 `bench` 标记的假数据（`clean` 子命令一键清理，`--tags` 超过 500 会截断），用法与注意事项见 README「数据规模支持」。
