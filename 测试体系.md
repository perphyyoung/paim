# 测试体系

三层测试（Rust 单测 / 前端单测 / e2e）的分工依据：**每一层只测「自己能可靠证明」的那一段**——同一行为不在多层重复断言内层细节；需要注入故障或延迟的场景，放在能够注入的那一层。

## 分工总览

| 层        | 命令           | 环境                        | 被测对象                                   |
| --------- | -------------- | --------------------------- | ------------------------------------------ |
| Rust 单测 | `pnpm test:rs` | cargo test，无 Tauri 运行时 | domain / infra：SQL 语义、领域规则、纯函数 |
| 前端单测  | `pnpm test:ui` | vitest，node 环境（无 DOM） | composable 与纯逻辑的状态机、竞态、容错    |
| e2e       | `pnpm e2e`     | Playwright CDP 连真实应用   | 真实后端 + 真实数据 + 真实 UI 的用户路径   |

`pnpm test` = 前端单测 + Rust 单测串联（前端在前，秒级失败即停）。

## 新增测试的选层流程

1. 数据语义 / SQL / 领域规则 → Rust 单测。
2. 前端状态机行为，且需要控制数据源时序（竞态、失败、延迟）→ 前端单测。
3. 真实集成路径（含 IPC 序列化往返、真实渲染、UI 流程）→ e2e。
4. 同一行为跨层时的分工：内层测逻辑全分支，e2e 只测「真实堆栈下可观察的那条主线」，不重复内层细节。

## Rust 单测

### 适用范围

- SQL 查询语义：分页 / 排序 / 筛选 / 特殊标签计数、部分索引、边界条件（空集合、offset 越界、上限钳制）。
- 领域规则：标签每域上限、备份导入的校验与容错、统计聚合口径。
- 纯函数：LIKE 转义、时间规整等确定性转换。
- 基础设施：连接管理、schema 演进、防呆逻辑。

### 能证明 / 局限

- 能证明：数据语义与边界正确；不依赖 Tauri 运行时，快、稳定、可并行。
- 局限：
  - 命令层是薄壳，「参数从 IPC 到 domain 的映射」由 tauri-specta 生成器保证形状，但生成的绑定与前端调用是否字段同名、语义一致，这层看不到；
  - UI 与前端状态机完全在视野之外。

## 前端单测

### 适用范围

- composable 的状态机行为：块级窗口（预取 / 淘汰 / 占位）、条件切换时丢弃过期响应、失败自动重试、在途请求去重、单条回写。
- **凡需要注入失败或延迟的场景一律放这层**：composable 的数据获取是显式注入的依赖，用可控的假数据源精确编排时序（第 N 次调用成功 / 失败 / 晚到），几行代码即可覆盖 e2e 里造不出的故障。
- 纯工具函数（与被测文件同目录，相对导入，不依赖 `@` 别名）。

### 为什么竞态与容错不放 e2e

真实 Tauri 的 IPC 层被注入脚本用 `Object.defineProperty` 冻结——`window.__TAURI_INTERNALS__` 本身及其每个成员都不可写、不可配置，页面 JS 既不能赋值替换也不能重新定义（官方 mocks 只在**非 Tauri 环境**可用，那里该对象是 mocks 自建的普通对象）。因此 e2e 里造不出「某次请求失败 / 晚到」，硬造只能改产品代码开后门。

### 能证明 / 局限

- 能证明：给定数据源的行为序列，前端状态机最终一致、不串台、失败可恢复。
- 局限：
  - node 环境不渲染真实 DOM，虚拟滚动的真实布局、事件、样式回归覆盖不到（由 e2e 的可见性断言兜底）；
  - 假数据源的参数 / 返回形状是测试自拟的，与真实 bindings 之间的一致性漂移（字段改名、类型变化）不会在这层暴露——由类型检查与 e2e 兜底。

## e2e

### 适用范围

- 真实用户路径：上传 / 导入 / 弹窗 / 批量操作等 UI 流程。
- 跨端集成：命令经真实 IPC 的序列化往返——前后端字段形状一致性的**唯一实测点**。
- 真实渲染：虚拟滚动的按需加载（占位骨架不渲染文字，「内容出现」即证明加载确实发生）。
- 只有真实环境才有的行为：CDP 连接、独立数据目录、文件对话框测试缝、多实例并行。

### 能证明 / 局限

- 能证明：真实堆栈拼起来确实能用。
- 局限：
  - 无法注入故障 / 延迟 → 竞态与容错路径覆盖不到（放前端单测，理由见上）；
  - 慢（每轮构建调试二进制 + 每个 spec 文件起独立实例），多实例并行有资源压力，存在环境性偶发失败——判定标准：单跑失败文件能通过 → 环境偶发，不算回归，不追查代码；
  - 断言只能依赖可观察结果（可见文本、语义定位器），内部状态（某请求是否发出）不可直接观测，需要时转化为可观察结果断言；
  - WebView2 专属运行时，跨浏览器差异不覆盖。

## 通用约定

- 测试文件与源文件平铺：Rust `*.test.rs`、前端 `*.test.ts`；e2e 按页面 / 专项分 spec 文件。
- 前端单测保持 node 环境（不引 jsdom）：一旦某条断言需要真实渲染，说明该行为属于 e2e 的适用范围。
- e2e 的日志埋点长期保留，噪声靠日志级别控制；应用代码的临时排查埋点定位后清理（见 [docs/日志使用说明.md](docs/日志使用说明.md)）。
- 失败排查顺序：Rust 单测 → 前端单测（快速定位逻辑层）→ e2e（定位集成层）；e2e 失败先单跑失败文件再下结论（见 [docs/e2e测试.md](docs/e2e测试.md) 的排查顺序）。
