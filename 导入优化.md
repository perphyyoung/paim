# 导入优化

> 记录 pm(prompt-manager)备份导入中缩略图重建的性能现状与备选方案。
> 当前已实现:多线程并发生成,线程数取系统可用并行度(`available_parallelism`)满核跑。
> 本文所列方案**暂不实施**,留作后续按需选择。

## 与 pm 的差距根源

pm 用 sharp(libvips):

- 编解码器为 C/SIMD 实现(libjpeg-turbo、libwebp、libspng);
- JPEG 支持 shrink-on-load:目标 200px 时按 1/2~1/8 的 DCT 缩放解码,大图只解一小部分像素;
- 流式处理,不全量展开位图。

paim 用 image crate(纯 Rust):

- 整图全量解码为内存位图后才缩放,图像越大,无效解码越多;
- 编解码器无 SIMD(image-webp、image 自带 JPEG 编码器),单张普遍慢 2~4 倍;
- 缩放本身(`thumbnail()`)很快,不是瓶颈;并发已取满核,高于 pm 的固定 5。

结论:单张速度差距来自编解码层,并发无法摊平。webp 没有缩放解码的格式支持,
pm 的优势对 webp 收窄为纯编解码器速度差距。

## 备选方案(暂不做)

### 方案一:缩略图编码换 jpeg-encoder

纯 Rust 但带 SIMD 的 JPEG 编码器 crate,替换 `build_thumbnail` 里 `DynamicImage::save`
的 JPEG 编码(image 自带编码器无 SIMD)。

- 改动最小:仅保存一处,无 C 依赖;
- 预期收益 10~30%(200×200 缩略图的编码占比随原图大小而下降)。

### 方案二:JPEG 解码换 turbojpeg(libjpeg-turbo 绑定)

解码是当前主要瓶颈,turbojpeg 可覆盖 JPEG 部分;webp 仍走 image-webp。

- 需要 C 工具链与 nasm,Windows 构建复杂度上升;
- 对 webp 为主的图库收益有限。

### 方案三:引 libvips 绑定(与 pm 完全同级)

rust libvips 绑定 + 随应用分发 libvips DLL,获得 shrink-on-load 与全部 SIMD 编解码器,
性能与 pm 持平。

- 依赖最重、发行最复杂;除非对导入速度有硬性要求,否则不建议。

## 备注

- 杀软实时扫描对写盘的影响双方同样存在(pm 在同样环境下依旧够快),
  不再作为排查方向,暂不加计时日志。
- 工作线程数取满核后,峰值内存为"每线程一张解码位图"
  (4K 图约 33MB,32 线程最坏约 1GB);若在低配机器上出现内存压力,
  可回退为限制线程数。
